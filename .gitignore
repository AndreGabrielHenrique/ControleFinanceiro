# CAMINHO RELATIVO: .gitignore
# Arquivo que especifica quais arquivos/diretórios o Git deve ignorar

# Veja https://help.github.com/articles/ignoring-files/ para mais sobre ignorar arquivos.

# dependencies - dependências do projeto
node_modules  # Ignora a pasta node_modules (contém todas as dependências instaladas)
.pnp          # Ignora pasta do PnP (Plug'n'Play) do Yarn
.pnp.js        # Ignora arquivo de configuração do PnP

# testing - testes
coverage      # Ignora pasta de relatórios de cobertura de testes

# production - produção
build         # Ignora pasta build gerada para produção

# docs - documentação
docs          # Ignora pasta de documentação (ADICIONADO)

# misc - diversos
.DS_Store      # Ignora arquivos do macOS (metadados do Finder)
.env.local     # Ignora arquivos de variáveis de ambiente locais
.env.development.local
.env.test.local
.env.production.local

# Logs de debug e erro do npm e yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*